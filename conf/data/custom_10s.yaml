data_root: "${root}/data/downstream/${data_name}"
X_path: "${data.data_root}/X.npy"
Y_path:  "${data.data_root}/Y.npy"
PID_path:  "${data.data_root}/pid.npy"
sample_rate: 30
task_type: 'classify'
dataset_name: 'EASE'
subject_count: -1
ratio2keep: 1
weighted_sample: true
batch_size: 50
held_one_subject_out: true
output_size: 4
weighted_loss_fn: true
